# TODO

## План исправления: WebSocket и отправка фото/голоса в чат

1. Проверить сборку и ASGI/Channels конфигурацию (asgi.py, установлен ли daphne/channels, корректна ли настройка CHANNEL_LAYERS).
2. Проверить URL для WebSocket и соответствие в шаблоне (ws/chat/<room_name>/). В частности: что приходит в room_name и совпадает ли тип.
3. Исправить/уточнить consumer: сейчас он сохраняет только `message` как текст и вообще не обрабатывает photo/voice.
4. Разобраться с формой отправки фото/голоса: в шаблоне `send_message.html` используется submit POST; consumer тут не участвует. Нужно проверить, куда POST редиректит и отображается ли фото/voice.
5. Проверить модели Direct/Message: опечатки в upload_to (например `mesage/`) не критично, но убедиться что поля `photo` и `voice` совпадают с form fields.
6. Проверить `chat/views.py` в обработчике `send_message`: сейчас Direct.objects.create(text=text, photo=photo, voice=voice). Но нужно убедиться, что шаблон реально посылает `photo` и `voice` и что они приходят как request.FILES.
7. Добавить/исправить server-side логику для voice/photo если планируется через WebSocket, либо явно разделить: фото/голос только через POST, текст только через WebSocket.
8. Добавить диагностику в консоль (логирование ошибок) и выполнить тесты вручную:
   - открыть /ws/chat/... и проверить ws onopen/onerror
   - отправить только текст (должно добавляться через websocket)
   - отправить фото (должно появляться)
   - записать голос и отправить (должно сохраняться и проигрываться)

